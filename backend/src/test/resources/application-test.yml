spring:
  datasource:
    url: jdbc:h2:mem:db_test;MODE=MySQL

custom:
  jwt:
    secret-key: test-secret-key-for-jwt-testing-only
    expire-seconds: "#{2}"
  refreshToken:
    expire-seconds: "#{10}"